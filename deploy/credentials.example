# Production Credentials - Store in /opt/maiqr/credentials/.env
# Replace these values with your actual production credentials

# Supabase Configuration
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-supabase-service-role-key

# Server Keys
SERVER_PRIVATE_KEY=your-production-private-key

# Note: This is just an example file. The actual credentials should be
# set up using the setup-credentials.sh script which will store them
# securely in /opt/maiqr/credentials/.env
